/target
Cargo.lock
# Local Claude editor state (settings.local.json, scheduled_tasks.lock).
# Repo-scoped skills live at `skills/<name>/` (first-class deliverable),
# not under `.claude/`. Global availability via a symlink from
# `~/.claude/skills/<name>` → `<repo>/skills/<name>`.
/.claude/
